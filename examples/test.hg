def say_hello_to(name) {
    puts("hello," + " " + name)
}

say_hello_to("yangtau")

def fib(n) {
    let a, b = 0, 1
    while b < n {
        a, b = b, a+b
        puts(a)
    }
}
puts("fib(100):")
fib(100)
puts()

def add(a, b) {
    return a + b
}

puts("3+4 =", add(3, 4))

def fac(n) {
    if n <= 1 {
        return 1
    } else {
        return fac(n-1) * n
    }
}

puts("fac(10) =", fac(10))

